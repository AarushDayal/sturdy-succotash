import React from 'react';

// This component is currently disabled in favor of the Hero 3D animation.
// Keeping a null render to prevent import errors if referenced.
export const ScrollEngineIntro: React.FC = () => {
  return null;
};